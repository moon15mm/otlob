# CLAUDE.md

اقرأ **[AGENTS.md](./AGENTS.md)** أولاً — هو ملف الذاكرة/السياق الكامل للنظام (البنية، القواعد الذهبية التي
يجب ألا تُكسر، تدفّق السلة، وخطوات النشر). ومرجع النشر التفصيلي في **[DEPLOYMENT.md](./DEPLOYMENT.md)**.

> باختصار: «اطلب» = SaaS مطاعم على واتساب، fork من «وردات». عملية واحدة، متعدد المستأجرين، سلة متعددة الأصناف.
> لا تُعد تسمية مفاتيح `wardat_*` في localStorage، ولا أسماء نماذج Prisma، ولا تكرّر `id` في HTML، وانشر تغييرات
> `public/` بـ `git pull` فقط. على الخادم لا تلمس غير `otlob-saas`.
